# DTMS Frontend Environment Variables
# Copy to .env.local for local development

# Backend API base URL
# In development, the Vite proxy forwards /api to http://localhost:5000
# In production (Vercel), set this to your deployed backend URL
# Example: https://dtms-backend.onrender.com/api
VITE_API_BASE_URL=/api
